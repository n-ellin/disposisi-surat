enum Role {
  kepsek,  // level_akses: kepsek
  pegawai, // level_akses: pegawai / admin (TU)
  tu,   // alias untuk pegawai — dipakai di splash_screen
  waka,    // level_akses: waka
  user,    // level_akses: user (guru, dll)
}