enum Role {
  kepsek,   // level_akses: kepsek
  pegawai,  // level_akses: pegawai
  waka,     // level_akses: waka (semua waka + koordinator waka)
  user,     // level_akses: user (semua kapro, bkk, bk, dll)
}